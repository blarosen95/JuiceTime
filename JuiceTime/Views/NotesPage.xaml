<Page
    x:Class="JuiceTime.Views.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JuiceTime.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource RegionBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Comments/Notes" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="30"/>
        <ScrollViewer Grid.Row="1">
            <TextBox x:Name="NotesTextBox" x:FieldModifier="internal" MaxHeight="100" Width="1000" HorizontalAlignment="Center" TextWrapping="Wrap" AcceptsReturn="True" IsSpellCheckEnabled="True" VerticalAlignment="Top" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </ScrollViewer>
    </Grid>
</Page>
